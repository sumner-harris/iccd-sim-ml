# Legacy source

These files are retained from the exploratory project so that the refactor
remains traceable. They are not imported by the `iccd_sim_ml` package. The
maintained implementations live under `src/iccd_sim_ml`.
