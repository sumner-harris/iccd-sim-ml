# Notebooks

`legacy/` contains sanitized snapshots of the original exploratory notebooks.
Outputs are cleared and machine-specific paths are placeholders. New
notebooks should import the tested functions in `iccd_sim_ml` instead of
duplicating physics, data loading, splitting, or model definitions in cells.
